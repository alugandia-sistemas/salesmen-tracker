# Dockerfile.frontend.dev - Para desarrollo LOCAL con docker-compose
# Este archivo instala TODAS las dependencias para npm run dev

FROM node:18-alpine

WORKDIR /app

# Copiar package.json
COPY frontend/package*.json ./

# Instalar dependencias (incluyendo vite, etc para desarrollo)
RUN npm cache clean --force && npm install --legacy-peer-deps

# Copiar código
COPY frontend/ .

# Exponer puerto
EXPOSE 5173

# Comando: npm run dev (Vite dev server con hot reload)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
